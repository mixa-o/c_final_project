#ifndef ADDITIONAL_H
#define ADDITIONAL_H

float calculate_speed(float s, float t);
float calculate_series(float x, int n); // Имя должно быть таким же везде!
void filter_start(int c, int *u_cnt, int *l_cnt);

#endif
